// This variable is used to save the background color menu reference.
// If it is present, the menu will be built "on load" and released "on unload"
// if this variable is deleted (please don't), the menu will have to be created (and deleted) each time the background color button is clicked.