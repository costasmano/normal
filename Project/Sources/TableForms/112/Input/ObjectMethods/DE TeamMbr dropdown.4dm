If (False:C215)
	//SC aTeamLeader on Form 
	//[InventoryPhotoInsp];"Input"
	//Pop up menu for team leader.
	Mods_2007_CM02
End if 
//ut_DebugtoClipboard (Current method name+" start at "+Char(9)+String(Milliseconds)+Char(13))
INV_TeamMember_OM